# from django import forms
# from .models import Explanation

# class CreateForm(forms.ModelForm): # DjangoのModelFormでは強力なValidationを使える

#     class Meta:
#         model = Explanation # モデルと接続しモデルの内容に応じてformを作ってくれる
#         fields = ('title', 'text', 'date') # 入力するカラムを指定
